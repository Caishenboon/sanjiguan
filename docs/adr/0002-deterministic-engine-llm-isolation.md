# ADR-0002：确定性引擎与 LLM 隔离

- 状态：Accepted
- 日期：2026-07-23

## 决策

八字、紫微、易经、评分、OHLC、候选竞争和敏感性聚合必须由确定性代码完成。LLM Gateway 只接收最小化后的规则结果和已审核知识片段，只返回符合 JSON Schema 的解释。

LLM 不得修改分数、创建证据、补齐中阴链断点、选择术数流派、生成具体前世身份或写回规则事实。Schema 校验失败最多修复一次，仍失败则退回规则模板。

## 验证

契约测试必须证明：供应商不可用时结构化分析仍完成；模型输出不能覆盖计算字段；提示注入文本仅被视作数据。
