# Physical three-coin technical debt

- The 64-hexagram asset is `review_candidate`; a qualified Yijing reviewer
  should independently verify every received-order name and trigram structure
  before any status change beyond research use.
- No interpretive rules, judgment texts, line texts, changed-line reading
  method, auspiciousness, or timing method has been reviewed or implemented.
- Legacy physical records without a persisted two-face mapping and raw numeric
  values cannot be replayed and remain `legacy_method_unknown`.
- The first UI mapping is heads=3 and tails=2. Future physical coin
  terminology must use a new explicit mapping ID rather than silently changing
  these labels.
- A future Six Signals mapping is a separate Sanjiguan research method and
  must not be represented as part of this traditional mechanical method.
- Owner-only authorization is enforced by the authenticated resource/API
  boundary; a future server-rendered Web session guard remains separate from
  the mechanical engine.

